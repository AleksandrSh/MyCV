GEMINI_API_KEY=your-key-from-aistudio.google.com
# Default: gemini-2.0-flash-lite (lighter free-tier usage). Use the same key/model as your other working site if needed.
GEMINI_MODEL=gemini-2.0-flash-lite
# Optional: only for custom domains (comma-separated)
# ALLOWED_ORIGINS=https://yourdomain.com
